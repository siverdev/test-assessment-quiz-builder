"use client";

export default function QuizForm() {

    // quiz title (mandatory)
    // quiz question (add, remove), but it must have at least 1 question
    // question can be of one of the types: boolean, input, checkbox
    //boolean has at least 2 options, input has no options, checkbox has at least 2 options as well 
    return (
        <form>

        </form>
    );
}
